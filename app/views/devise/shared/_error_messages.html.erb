<% if resource.errors.any? %>
  <% title = "Cannot save #{resource.class.model_name.human.downcase}" %>
  <% resource.errors.full_messages.each do |message| %>
    <%= render NotificationComponent.new(message: message, type: "danger", title: title) %>
  <% end %>
<% end %>
