<p id="notice"><%= notice %></p>

<h1>Cards</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Name</th>
      <th>Number</th>
      <th>Expiration</th>
      <th>Status</th>
      <th>Card category</th>
      <th>Manager</th>
      <th>Cvv</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @cards.each do |card| %>
      <tr>
        <td><%= card.user_id %></td>
        <td><%= card.name %></td>
        <td><%= card.number %></td>
        <td><%= card.expiration %></td>
        <td><%= card.status %></td>
        <td><%= card.card_category_id %></td>
        <td><%= card.manager %></td>
        <td><%= card.cvv %></td>
        <td><%= link_to 'Show', card %></td>
        <td><%= link_to 'Edit', edit_card_path(card) %></td>
        <td><%= link_to 'Destroy', card, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Card', new_card_path %>
